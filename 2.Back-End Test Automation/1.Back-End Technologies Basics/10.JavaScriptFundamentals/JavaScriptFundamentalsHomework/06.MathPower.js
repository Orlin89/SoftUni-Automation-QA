function mathPow(number, power)
{
    let result = Math.pow(number, power);
    console.log(result);
}





function mathPow2(number, power)
{
    let result = number ** power;
    console.log(result);
}
